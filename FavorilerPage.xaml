<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fitnessapp.FavorilerPage"
             BackgroundColor="Black"
             Shell.NavBarIsVisible="False"
             NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="Auto, *">

        <Border Grid.Row="0" BackgroundColor="#1A1A1A" StrokeThickness="0" Padding="15,15" Margin="0,0,0,10">
            <Grid ColumnDefinitions="Auto, *">
                <Border Grid.Column="0" BackgroundColor="Transparent" Stroke="White" StrokeThickness="1"
                        HeightRequest="36" WidthRequest="36" StrokeShape="RoundRectangle 18" VerticalOptions="Center">
                    <Label Text="←" TextColor="White" FontSize="24" HorizontalOptions="Center" VerticalOptions="Center" TranslationY="-2"/>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="GeriDon_Clicked"/>
                    </Border.GestureRecognizers>
                </Border>
                <Label Grid.Column="1" Text="FAVORİLERİM" TextColor="White" FontSize="20" FontAttributes="Bold"
                       HorizontalOptions="Center" VerticalOptions="Center" Margin="-36,0,0,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1">

            <VerticalStackLayout x:Name="BosEkranUyarisi" Spacing="20" VerticalOptions="Center" HorizontalOptions="Center" IsVisible="False">
                <Label Text="💔" FontSize="60" HorizontalOptions="Center"/>
                <Label Text="Henüz favori hareketin yok." TextColor="Gray" FontSize="16" HorizontalOptions="Center"/>
                <Label Text="Antrenmanlardan beğendiklerini kalp ikonuna basarak ekleyebilirsin." 
                       TextColor="#666" FontSize="12" HorizontalOptions="Center" HorizontalTextAlignment="Center" WidthRequest="250"/>
            </VerticalStackLayout>

            <CollectionView x:Name="cvKategoriler" SelectionMode="Single" SelectionChanged="KategoriSecildi">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2" HorizontalItemSpacing="10" VerticalItemSpacing="10"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeShape="RoundRectangle 15" StrokeThickness="0" BackgroundColor="#1A1A1A" HeightRequest="120" Padding="15">
                            <Grid RowDefinitions="*, Auto, Auto">
                                <Border Grid.Row="0" BackgroundColor="#333" WidthRequest="40" HeightRequest="40" StrokeShape="RoundRectangle 20" HorizontalOptions="Start" VerticalOptions="Start">
                                    <Label Text="{Binding BasHarf}" TextColor="#FFD700" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center"/>
                                </Border>

                                <Label Grid.Row="1" Text="{Binding KategoriAdi}" TextColor="White" FontSize="16" FontAttributes="Bold" VerticalOptions="End"/>

                                <Label Grid.Row="2" Text="{Binding HareketSayisi, StringFormat='{0} Hareket'}" TextColor="Gray" FontSize="12" VerticalOptions="End"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>